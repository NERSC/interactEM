
/**
 * @file op_sender.hpp
 * @brief Handles message sending from operators on a node to the RDMA proxy
 * 
 * OpSender manages local operator-to-proxy communication, implementing message queuing
 * and forwarding from local operators to the RDMA proxy service.
 */

#include <iostream>
#include <string>
#include <thallium.hpp>

namespace interactEM {

class OperatorSender {

    private:

    public:
        OperatorSender() = default; 

        // void initialize();
        // void stop();

};

}